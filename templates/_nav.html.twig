<nav class="navbar-custom fixed-top">
    <div class="navbar-inner my-2">

        <!-- Hamburguesa para Móvil -->
        <input type="checkbox" id="nav-toggle" class="nav-toggle-checkbox d-none">
        <label for="nav-toggle" class="nav-toggle-label d-lg-none">
            <i class="bi bi-list fs-1 text-white"></i>
        </label>

        <!-- IZQUIERDA: LOGO -->
        <a class="navbar-brand-custom" href="{{ path('home') }}">
            {% if personalize and personalize.logo %}
                <img src="{{ asset('uploads/logo/' ~ personalize.logo) }}" alt="Logo" height="64" class="navbar-logo">
            {% else %}
                <span class="brand-text">{{ personalize.companyName ?? 'Empresa' }}</span>
            {% endif %}
        </a>

        <!-- MENÚ COLAPSABLE (CENTRO + DERECHA) -->
        <div class="navbar-collapsible">
            
            <!-- CENTRO: CATEGORÍAS + BUSCADOR -->
            <div class="navbar-center">
                <form action="{{ path('product') }}" method="GET" class="search-wrapper">
                    <div class="search-modern-custom">
                        <i class="bi bi-search search-icon"></i>
                        <input type="search" name="string" placeholder="Buscar productos…" autocomplete="off">
                    </div>
                </form>
                
                <div class="dropdown">
                    <button class="nav-pill-btn"
                            type="button"
                            id="dropdownMenuCategories"
                            data-toggle="dropdown"
                            aria-haspopup="true"
                            aria-expanded="false">
                        <span>Categorías</span>
                        <i class="bi bi-chevron-down dropdown-arrow-icon"></i>
                    </button>

                    <div class="dropdown-menu dropdown-menu-styled shadow-xl dropdown-animated"
                         aria-labelledby="dropdownMenuCategories">
                        <a class="dropdown-item dropdown-item-styled fw-semibold" href="{{ path('product') }}">
                            <b>Todas las categorías</b>
                        </a>
                        <div class="dropdown-divider"></div>
                        {% for category in allCategories %}
                            <a class="dropdown-item dropdown-item-styled"
                               href="{{ path('product', {'categories[]': category.id}) }}">
                                <i class="bi bi-tag me-2 opacity-50 mx-2"></i>{{ category.name }}
                            </a>
                        {% endfor %}
                    </div>
                </div>
                <div class="navbar-right">

                {% if app.user %}
                    <div class="dropdown">
                        <a class="nav-user-btn text-white"
                           href="#"
                           role="button"
                           data-toggle="dropdown"
                           aria-haspopup="true"
                           aria-expanded="false">
                            <div class="user-avatar">{{ app.user.firstname|slice(0,1)|upper }}</div>
                            <span class="d-none d-lg-inline">{{ app.user.firstname }}</span>
                            <i class="bi bi-chevron-down dropdown-arrow-icon"></i>
                        </a>

                        <ul class="dropdown-menu dropdown-menu-right dropdown-menu-styled shadow-xl dropdown-animated">
                            <li class="dropdown-user-header">
                                <div class="user-avatar user-avatar-lg">{{ app.user.firstname|slice(0,1)|upper }}</div>
                                <div>
                                    <div class="fw-semibold">{{ app.user.firstname }}</div>
                                    <small class="text-muted">Mi cuenta</small>
                                </div>
                            </li>
                            <li><div class="dropdown-divider"></div></li>
                            <li><a class="dropdown-item dropdown-item-styled" href="{{ path('account') }}"><i class="mx-2 bi bi-person me-2"></i>Mi Perfil</a></li>
                            <li><a class="dropdown-item dropdown-item-styled" href="{{ path('account_orders') }}"><i class="mx-2 bi bi-bag-check me-2"></i>Mis Pedidos</a></li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li><div class="dropdown-divider"></div></li>
                                <li><a class="dropdown-item dropdown-item-styled" href="/admin"><i class="mx-2 bi bi-speedometer2 me-2"></i>Administración</a></li>
                            {% endif %}
                            <li><div class="dropdown-divider"></div></li>
                            <li><a class="dropdown-item dropdown-item-styled text-danger" href="{{ path('app_logout') }}"><i class="mx-2 bi bi-box-arrow-right me-2"></i>Cerrar Sesión</a></li>
                        </ul>
                    </div>
                {% else %}
                    <a class="btn-login" href="{{ path('app_login') }}">
                        <i class="bi bi-person me-1"></i>Ingresar
                    </a>
                {% endif %}

                <a href="{{ path('cart') }}" class="cart-btn text-white" aria-label="Ver carrito">
                    <i class="bi bi-bag fs-5"></i>
                    {% if cartCount > 0 %}
                        <span class="cart-badge-custom">{{ cartCount }}</span>
                    {% endif %}
                </a>
            </div>
            </div>

            
        </div>
    </div>
</nav>

<style>
/* =============================================
   NAVBAR BASE & DESKTOP
   ============================================= */
.navbar-custom {
    background: var(--primary-color) !important;
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 4px 25px rgba(0, 0, 0, 0.15);
    z-index: 1030;
}
.navbar-inner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0.65rem 2rem;
}

.navbar-collapsible {
    display: flex;
    align-items: center;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.navbar-brand-custom {
    text-decoration: none;
    flex-shrink: 0;
    transition: opacity 0.2s, transform 0.2s;
    display: flex;
    align-items: center;
}
.navbar-brand-custom:hover { opacity: 0.8; transform: scale(1.02); }
.navbar-logo {
    border-radius: 50%;
    width: 64px;
    height: 64px;
    object-fit: cover;
    border: 2px solid rgba(255, 255, 255, 0.2);
}
.brand-text { font-size: 1.3rem; font-weight: 800; color: #fff; letter-spacing: -0.5px; }

.navbar-center {
    display: flex;
    align-items: center;
    gap: 2rem;
}

.navbar-right {
    display: flex;
    align-items: center;
    gap: 2rem;
    flex-shrink: 0;
}

.search-wrapper { width: 350px; min-width: 0; }
.search-modern-custom { position: relative; display: flex; align-items: center; }
.search-icon {
    position: absolute; left: 14px;
    color: rgba(255, 255, 255, 0.7); 
    font-size: 0.9rem;
    pointer-events: none;
    transition: color 0.2s;
}
.search-modern-custom input {
    width: 100%;
    padding: 0.55rem 1rem 0.55rem 2.6rem;
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 999px;
    font-size: 0.9rem;
    color: #fff;
    outline: none;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

.nav-pill-btn {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: #fff;
    border-radius: 999px;
    padding: 0.5rem 1.25rem;
    font-size: 0.875rem;
    font-weight: 600;
    cursor: pointer;
    white-space: nowrap;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

.nav-user-btn {
    display: flex; align-items: center; gap: 0.6rem;
    text-decoration: none; color: #fff;
    border-radius: 999px;
    padding: 0.35rem 1rem 0.35rem 0.35rem;
    border: 1px solid transparent;
    font-size: 0.9rem; font-weight: 600;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

.cart-btn {
    position: relative;
    display: flex; align-items: center; justify-content: center;
    width: 44px; height: 44px; border-radius: 50%;
    color: #fff; border: 1px solid transparent; text-decoration: none;
    transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
}

/* =============================================
   DROPDOWNS & ANIMATIONS
   ============================================= */
.dropdown-menu-styled {
    border: 1px solid rgba(0,0,0,0.05) !important;
    border-radius: 16px !important;
    padding: 0.6rem !important;
    min-width: 240px;
    margin-top: 10px !important;
    overflow: hidden;
    z-index: 1050;
}

.dropdown-item-styled {
    border-radius: 10px;
    font-size: 0.9rem;
    padding: 0.6rem 1rem !important;
    color: #4b5563;
    transition: all 0.2s ease;
    display: flex; align-items: center;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

@media (min-width: 992px) {
    .dropdown-animated {
        display: block !important;
        visibility: hidden;
        opacity: 0;
        transform: translateY(-10px) scale(0.96);
        pointer-events: none;
        transition: opacity 0.25s ease, transform 0.25s cubic-bezier(0.16, 1, 0.3, 1), visibility 0.25s;
    }
    .dropdown.show .dropdown-animated {
        visibility: visible;
        opacity: 1;
        transform: translateY(0) scale(1);
        pointer-events: auto;
    }
}

/* =============================================
   ESTILOS RESPONSIVE (MÓVIL) - AL FINAL PARA PRIORIDAD
   ============================================= */
@media (max-width: 991px) {
    .navbar-inner {
        padding: 0.5rem 1.25rem;
        justify-content: space-between !important;
    }
    
    .navbar-logo { width: 48px; height: 48px; }

    .navbar-collapsible {
        position: fixed;
        top: 80px; left: 0; width: 100%; height: calc(100vh - 80px);
        background: var(--primary-color);
        flex-direction: column;
        justify-content: flex-start;
        align-items: stretch;
        padding: 1.5rem; gap: 1rem;
        transform: translateX(-100%);
        visibility: hidden;
        overflow-y: auto;
        z-index: 1040;
        transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), visibility 0.4s;
    }

    #nav-toggle:checked ~ .navbar-collapsible {
        transform: translateX(0);
        visibility: visible;
    }

    /* CONTENEDOR CENTRAL: BUSCADOR + CATEGORÍAS */
    .navbar-center {
        display: flex !important;
        flex-direction: column !important;
        width: 100% !important;
        max-width: 100% !important;
        gap: 0 !important;
    }
    
    .search-wrapper {
        order: 1;
        width: 100% !important;
        margin: 0 !important;
    }
    
    .navbar-center .dropdown {
        order: 2;
        width: 100% !important;
        margin: 0 !important;
    }

    .search-modern-custom input {
        border-radius: 16px 16px 0 0 !important;
        border: 1px solid rgba(255, 255, 255, 0.1) !important;
        height: 54px;
        width: 100% !important;
    }

    .nav-pill-btn {
        width: 100% !important;
        height: 54px;
        border-radius: 0 0 16px 16px !important;
        border: 1px solid rgba(255, 255, 255, 0.1) !important;
        border-top: none !important;
        background: rgba(255, 255, 255, 0.15) !important;
        justify-content: center;
        font-size: 1rem;
        transition: border-radius 0.2s;
    }

    .dropdown.show .nav-pill-btn {
        border-radius: 0 !important; /* Cuadrado para unir con el dropdown */
    }

    /* CONTENEDOR DERECHO: USUARIO + CARRITO */
    .navbar-right {
        order: 3;
        width: 100% !important;
        display: flex !important;
        margin-top: 2rem;
        background: rgba(255,255,255,0.1);
        border-radius: 18px;
        border: 1px solid rgba(255,255,255,0.15);
        padding: 6px;
        gap: 6px;
        position: relative;
    }
    
    .navbar-right > .dropdown,
    .navbar-right > .cart-btn {
        flex: 1 !important;
        height: 60px;
        display: flex;
        justify-content: center;
        align-items: center;
        border-radius: 14px !important;
        transition: all 0.2s ease;
        text-decoration: none;
        margin: 0 !important;
    }

    .navbar-right > .cart-btn:active,
    .navbar-right > .dropdown.show {
        background: rgba(255,255,255,0.12);
    }

    .navbar-right i {
        font-size: 1.5rem !important;
    }

    .user-avatar {
        width: 38px !important;
        height: 38px !important;
        font-size: 1rem !important;
        border: 2px solid rgba(255,255,255,0.2);
    }

    .btn-login, .nav-user-btn, .cart-btn {
        width: 100% !important;
        height: 64px !important;
        background: transparent !important;
        border: none !important;
        border-radius: 0 !important;
        padding: 0 !important;
    }

    /* DROPDOWNS EN MÓVIL */
    .navbar-collapsible .dropdown-menu-styled {
        position: static !important;
        width: 100% !important; 
        max-width: none !important;
        margin: 0 !important; 
        margin-top: 0 !important; /* Forzar pegado superior */
        display: none;
        background: #fff !important;
        border-radius: 0 0 16px 16px !important;
        padding: 0.5rem !important;
        box-shadow: 0 12px 30px rgba(0,0,0,0.15) !important;
        border: 1px solid rgba(0,0,0,0.1) !important;
        border-top: none !important;
        transform: none !important;
        top: 0 !important;
    }

    /* El dropdown del usuario debe ser absoluto para no romper la fila 50/50 */
    .navbar-right .dropdown-menu-styled {
        position: absolute !important;
        top: 100% !important;
        left: 0 !important;
        width: 100% !important;
        margin-top: 0 !important; /* Pegado total inferior */
        border-radius: 0 0 16px 16px !important;
        border: 1px solid #eee !important;
        border-top: none !important;
        z-index: 2000;
        transform: none !important;
    }

    .navbar-collapsible .dropdown.show .dropdown-menu-styled {
        display: block !important;
    }

    .navbar-collapsible .dropdown-item-styled {
        font-size: 0.8rem !important;
        padding: 0.6rem 0.75rem !important;
        white-space: nowrap !important;
        overflow: hidden !important;
        text-overflow: ellipsis !important;
    }
    
    .nav-user-btn span { display: none !important; }
}
</style>